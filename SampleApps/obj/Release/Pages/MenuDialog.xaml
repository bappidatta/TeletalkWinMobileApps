<ContentDialog
    x:Class="SampleApps.Pages.MenuDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SampleApps.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Black"
    Title="" Opacity="0.7">

    <ScrollViewer Grid.Row="2" Grid.ColumnSpan="2"
        HorizontalAlignment="Right"
        VerticalAlignment="Center"
        VerticalScrollBarVisibility="Visible" 
        VerticalScrollMode="Enabled"
        Margin="0,100,0,0" Width="300"
        HorizontalScrollBarVisibility="Auto" Opacity="0.7">
        <ListView x:ConnectionId='1' Name="menuListView" 
                  DataContext="{Binding}"
                                              
                      IsItemClickEnabled="True"
                                                        
                  ScrollViewer.HorizontalScrollBarVisibility="Auto">

            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Height="40" Margin="10">
                        <Image Source="{Binding image}"
                                   Height="30"
                                   Width="30"
                                   Margin="15,0"/>

                        <TextBlock Text="{Binding name}"
                               Foreground="#55d5f9"
                               FontSize="22"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ScrollViewer>
</ContentDialog>

